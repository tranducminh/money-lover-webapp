export enum CategoryType {
  EXPENSE,
  INCOME,
  LOAN,
  RECOVER_DEBT,
  DEBT,
  BACK_DEBT,
}

export enum MessageType {
  INFO = "info",
  SUCCESS = "success",
  WARNING = "warning",
  ERROR = "error",
}

export enum WalletTab {
  TRANSACTION = "transaction",
  MEMBERS = "members",
}
